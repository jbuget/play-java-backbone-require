/*
var assert = require('assert'),
    ProductList = requirejs('catalog/ProductList'),
    ProductListView = requirejs('catalog/ProductListView');

describe('ProductListView', function() {

    var products,
        productListView;

    before(function () {
        products = new ProductList();
        productListView = new ProductListView({collection: products});
    });

    describe('#render()', function() {

        before(function() {
            productListView.render();
            $("#app").html(this.currentView.el);
        });

        it('should display as many products as fetched', function() {
        });

        it('should display products information: image / name / price', function() {
        });

        it('should display an "Add to cart" button with product ID data-tag', function() {
        });


    });
});
*/


